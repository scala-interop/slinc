package fr.hammons.slinc.modules

import munit.FunSuite
import fr.hammons.slinc.modules.ReadWriteModule

class ReadWriteModuleSpec extends FunSuite {

  test("unionReader returns a reader that behaves as expected") {
    // Create a TypeDescriptor
    // Call unionReader with the descriptor
    // Assert that the returned reader behaves as expected
  }

  test("unionWriter returns a writer that behaves as expected") {
    // Create a TypeDescriptor
    // Call unionWriter with the descriptor
    // Assert that the returned writer behaves as expected
  }

  test("write writes to memory as expected") {
    // Create a Mem, Bytes, and TypeDescriptor
    // Call write with those inputs
    // Assert that the memory was written to as expected
  }

  // Similar tests for writeArray, read, readArray, and readFn
}